<?php

use Illuminate\Database\Seeder;
use Spatie\Permission\Models\Role;
use Spatie\Permission\Models\Permission;

class DatabaseSeeder extends Seeder
{
    /**
     * Run the database seeds.
     *
     * @return void
     */
    public function run()
    {
        // $this->call(UsersTableSeeder::class);
    // $role = Role::create(['name' => 'admin']);
    // $permission = Permission::create(['name' => 'manage managers']);
    // $permission1 = Permission::create(['name' => 'manage receptionists']);
    // $permission2 = Permission::create(['name' => 'manage clients']);
    // $role1 = Role::create(['name' => 'manager']);
    // $role2 = Role::create(['name' => 'receptionist']);
    // $role->givePermissionTo($permission);
    // $role->givePermissionTo($permission1);
    // $role->givePermissionTo($permission2);
    // $role1->givePermissionTo($permission1);
    // $role1->givePermissionTo($permission2);
    // $role2->givePermissionTo($permission2);

    ////$user->can('edit articles');
    ////$user->assignRole('writer');
    }
}
